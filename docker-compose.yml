version: "3.9"
services:
  invoice-portal:
    build: .
    image: invoice-portal:latest
    container_name: invoice-portal
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

